🕌 Namaz Bot with Monitoring
Мусульманский бот для уведомлений о времени намазов с полным стеком мониторинга, авто-тестами и CI/CD.

✨ Особенности
⏰ Автоматические уведомления за 5 минут до каждого намаза

🚰 Напоминание об омовении в каждом сообщении

📊 Полный мониторинг через Grafana + Loki

⚡ Асинхронная обработка через Apache Kafka

🔔 Telegram интеграция с красивым форматированием

📈 Логирование и аналитика в реальном времени

✅ Автоматические тесты с покрытием >80%

🚀 CI/CD пайплайн с авто-деплоем

🐳 Docker контейнеризация

🏗️ Архитектура
text
┌─────────────────┐    ┌─────────────┐    ┌─────────────┐    ┌──────────────┐
│   Namaz Bot     │───▶│   Kafka     │───▶│   Loki      │───▶│   Grafana    │
│   (Python)      │    │   (Broker)  │    │   (Logs)    │    │   (UI)       │
└─────────────────┘    └─────────────┘    └─────────────┘    └──────────────┘
         │                      │                    │                 │
         ▼                      ▼                    ▼                 ▼
┌─────────────────┐    ┌─────────────┐    ┌─────────────┐    ┌──────────────┐
│   Telegram API  │    │   Zookeeper │    │   Promtail  │    │   Dashboard  │
│                 │    │             │    │             │    │              │
└─────────────────┘    └─────────────┘    └─────────────┘    └──────────────┘
         │                      │                    │                 │
         ▼                      ▼                    ▼                 ▼
┌─────────────────┐    ┌─────────────┐    ┌─────────────┐    ┌──────────────┐
│   GitHub Actions│    │   Pytest    │    │   Coverage  │    │   Auto-Deploy│
│   (CI/CD)       │    │   (Tests)   │    │   Reports   │    │   to Server  │
└─────────────────┘    └─────────────┘    └─────────────┘    └──────────────┘
🚀 Быстрый старт
1. Клонирование репозитория
bash
git clone https://github.com/amalnurg/namaz-bot-monitoring.git
cd namaz-bot-monitoring
2. Установка зависимостей
bash
# Установка Python зависимостей
pip install -r requirements.txt

# Установка зависимостей для разработки
pip install -r requirements-dev.txt
3. Настройка конфигурации
Создайте файл .env:

env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
CITY=Ufa
COUNTRY=Russia
KAFKA_BROKER=localhost:9092
4. Запуск бота
bash
python3 namaz_bot.py
🧪 Тестирование
Запуск всех тестов
bash
# Запуск тестов с покрытием
pytest --cov=./ --cov-report=html

# Запуск тестов с подробным выводом
pytest -v

# Запуск конкретного теста
pytest tests/test_prayer_times.py -v
Структура тестов
text
tests/
├── test_prayer_times.py      # Тесты получения расписания
├── test_telegram_bot.py      # Тесты Telegram интеграции
├── test_kafka_integration.py # Тесты Kafka
├── test_notification_logic.py # Тесты логики уведомлений
└── conftest.py              # Фикстуры и настройки
Покрытие кода
bash
# Генерация отчета о покрытии
pytest --cov=./ --cov-report=term-missing

# HTML отчет (откроется в браузере)
pytest --cov=./ --cov-report=html && open htmlcov/index.html
🔄 CI/CD Pipeline (GitHub Actions)
Автоматические процессы:
При пуше в main:

✅ Запуск всех тестов

✅ Проверка покрытия кода (>80%)

✅ Проверка линтером (flake8)

✅ Авто-деплой на сервер

При создании Pull Request:

✅ Тестирование изменений

✅ Проверка качества кода

✅ Генерация отчетов

Просмотр CI/CD:
GitHub Actions: .github/workflows/ci-cd.yml

Статус сборки: На вкладке "Actions" репозитория

Артефакты: Отчеты о тестировании и покрытии

🐳 Docker развертывание
Полный стек (Grafana + Loki + Kafka + Bot)
bash
# Запуск всего стека
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f
Только бот
bash
# Сборка образа
docker build -t namaz-bot .

# Запуск контейнера
docker run -d \
  --name namaz-bot \
  --env-file .env \
  namaz-bot
⚙️ Конфигурация сервисов
Systemd служба (для продакшена)
bash
# Установка службы
sudo cp systemd/namaz-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable namaz-bot
sudo systemctl start namaz-bot

# Просмотр статуса
sudo systemctl status namaz-bot

# Просмотр логов
sudo journalctl -u namaz-bot -f

📁 Структура проекта
text
namaz-bot-monitoring/
├── namaz_bot.py              # Основной код бота
├── namaz_bot_kafka.py        # Версия с Kafka интеграцией
├── requirements.txt          # Python зависимости
├── requirements-dev.txt      # Зависимости для разработки
├── docker-compose.yml        # Docker компоновка
├── Dockerfile               # Docker образ бота
├── promtail-config.yml      # Конфиг для сбора логов
├── systemd/
│   └── namaz-bot.service    # Systemd служба
├── tests/                   # Авто-тесты
│   ├── test_prayer_times.py
│   ├── test_telegram_bot.py
│   ├── test_kafka_integration.py
│   ├── test_notification_logic.py
│   └── conftest.py
├── .github/workflows/       # CI/CD пайплайны
│   └── ci-cd.yml
├── .gitignore
├── README.md
└── .env.example            # Пример конфигурации
🛠️ Разработка
Установка для разработки
bash
# Установка в режиме разработки
pip install -e .

# Установка pre-commit хуков
pre-commit install

# Запуск линтера
flake8 .
Code Style
Форматирование: black

Линтинг: flake8

Типизация: mypy (опционально)

Pre-commit хуки: автоматическая проверка перед коммитом

📊 Метрики и мониторинг
Доступные метрики:
Количество отправленных уведомлений

Время до ближайшего намаза

Статус API соединений

Потребление памяти/CPU

Ошибки и исключения

Дашборды Grafana:
Основной дашборд: Общая статистика бота

Мониторинг Kafka: Очереди и сообщения

Логи в реальном времени: Просмотр логов бота

Метрики системы: CPU, память, сеть

📞 Контакты
Amal Nurgaleev - @amalnurg

Ссылка на проект: https://github.com/amalnurg/namaz-bot-monitoring

⭐ Если вам понравился проект, поставьте звезду на GitHub! ⭐
